import type { CSSProperties } from "react";
import { colors, fontFamily } from "./tokens";

export const app: CSSProperties = { fontFamily, minHeight: "100vh", background: colors.slate50, color: colors.slate800 };
