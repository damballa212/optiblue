import type { CSSProperties } from "react";
import { colors } from "../../styles/tokens";

export const destacadosWrap: CSSProperties = { background: colors.white, borderTop: `1px solid ${colors.slate200}` };
export const serviciosWrap: CSSProperties = { background: colors.blueBg };
export const ctaCenter: CSSProperties = { textAlign: "center", marginTop: 24 };
export const ctaCenterSecondary: CSSProperties = { textAlign: "center", marginTop: 20 };
